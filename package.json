{
  "name": "kiipeli",
  "version": "0.1.0",
  "engines": {
    "node": "16.x",
    "npm": "8.x"
  },
  "scripts": {
    "start": "cd backend && node index.js",
    "heroku-postbuild": "cd backend && npm install && cd ../frontend && npm install --force && npm run build"
}
}